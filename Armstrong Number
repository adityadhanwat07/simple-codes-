#include <stdio.h>
#include <math.h>
int main() {
    int n=153, temp=n, sum=0;
    while(temp>0){
        int d=temp%10;
        sum+=d*d*d;
        temp/=10;
    }
    if(sum==n) printf("Armstrong");
    else printf("Not Armstrong");
    return 0;
}
