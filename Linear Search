
#include <stdio.h>
int main(){
    int a[]={1,2,3,4},key=3,found=0;
    for(int i=0;i<4;i++) if(a[i]==key){ found=1; break; }
    if(found) printf("Found");
    else printf("Not Found");
    return 0;
}
